const express = require('express');
const router = express.Router();

const noticeCreateMiddleware = (req, res, next) => {
    /*
    req: {
        title: " ",
        content: " ",
    }
    */
   
}

module.exports = noticeCreateMiddleware;